# 剪影工坊 CutFlow

面向 Windows 和 macOS 的轻量视频剪切、拼接和转码工具。

## 开发

```bash
pnpm install
pnpm dev
```

## 打包

```bash
pnpm build:win
pnpm build:mac
```

> macOS 安装包需要在 macOS 机器上构建；正式分发建议配置 Apple Developer 签名与公证。
